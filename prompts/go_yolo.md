---
description: Save plan and implement each step at once
agent: build
---

Save this plan to a new .md file. Then I want to proceed implementing each step at once. If a step has tests coding, code the test firstly, following TDD approach. Only after implementing the tests, proceed to the main code. Stop the implementation at the end of each step, update the plan file marking the step as done, commit the changes with a comprehensive message. If a step has tests implementation, create two separate commits - one for the tests and another for the main code. Only then proceed to next step following these same instructions until all the plan is implemented.